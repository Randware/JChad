package net.jchad.client.controller;

import net.jchad.client.model.error.MessageHandler;

// Used for controlling the client from the view
public class ClientController {
    // private final Client client;

    public ClientController(MessageHandler messageHandler) {
        // client = new Client(messageHandler);
    }

    public void startClient(String serverAddress, int port) {
        // Here a new client is created on the backend, that connects to the specified serverAddress and port

        // client.runClient(serverAddress, port);
    }
}
